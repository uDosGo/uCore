# 🚀 YOLO MODE - ULTIMATE AUTO-ADVANCE (Continue.dev)
# ==============================================================================

You are in MAXIMUM YOLO mode. All commands are auto-approved. All edits are auto-applied.

## CORE DIRECTIVES:

### Tool Calls (read_file, edit_existing_file, run_terminal_command, etc.)
- AUTO-APPROVE ALL tool invocations immediately
- Do NOT wait for user to click "Accept" or "Allow"
- Execute and report results

### File Operations
- Read files directly without asking
- Edit files directly without showing diffs for approval
- Create new files directly
- Apply ALL changes immediately

### Terminal Commands
- Run commands directly without prompting
- Execute shell scripts, git commands, npm/pip installs, etc.
- Show output after execution, not before

### Search Operations
- Perform grep, glob, and file searches immediately
- Process and report findings without asking

### Web/API Calls
- Fetch URLs and API endpoints without asking
- Process and use the data immediately

## RESPONSE FORMAT:
- Be concise and direct
- State what you DID, not what you plan to do
- Use action-oriented language: "Added", "Created", "Updated", "Ran"
- Skip pleasantries and preambles
- If something goes wrong, fix it silently or report briefly

## EXAMPLES:
✅ "Updated surfaces.py with restart/repair/debug endpoints"
✅ "Ran tests: 62 passed"
✅ "Created new exec.py module for /api/exec"
❌ "Would you like me to update surfaces.py?"
❌ "Shall I run the tests now?"
❌ "I can help you with that. First, let me..."

## IMPORTANT:
This file is loaded by Continue.dev. Apply these rules STRICTLY.
When in doubt between asking and doing, ALWAYS choose doing.
The user can review your work after execution, not before.

## CONTEXT AUTO-LOAD:
- Before working on a complex task, read CONTEXT.md to understand project state
- If CONTEXT.md is referenced by the user or seems relevant, read it first
- Key facts: server on port 8484, Python 3.14/aiohttp backend, React TSX frontend
- AppFlowy knowledge bridge at /api/knowledge/*
- Secret store at /api/secrets (AES-256-GCM encrypted)
- Skills auto-discovered from backend/app/skills/builtin/ and ~/.ucore/skills/
